<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="m.com.service">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".PersonalActivity"/>
        <activity android:name=".BannerActivity"/>
        <activity android:name=".Page.FirstActivity"/>
        <activity android:name=".Page.SecondActivity"/>
        <activity android:name=".Page.ThirdActivity"/>
        <activity android:name=".Page.FourthActivity"/>
        <activity android:name=".Page.AboutActivity"/>

        <activity android:name=".JudgeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>